# Stable environment used for the verified Week 3 run.
# Install the CUDA 12.6 PyTorch wheels from the official PyTorch index first.
torch==2.10.0
torchvision==0.25.0
ultralytics==8.4.90
opencv-python>=4.8,<5
matplotlib>=3.7,<4
PyYAML>=6.0,<7
numpy>=1.23
tqdm>=4.65
reportlab>=4.0
pypdf>=5.0
pdfplumber>=0.11

# Optional deployment backends used for the laptop benchmark are platform-specific
# and are therefore not forced by this core file:
# onnx==1.21.0, onnxruntime-gpu==1.22.0, tensorrt-cu12==11.1.0.106
